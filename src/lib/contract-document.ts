export const CONTRACT_DATA_VERSION = 1;
export const ACKNOWLEDGEMENTS = ["Pool access", "Natural finish variation", "Roof and drainage", "Decking variation", "Startup kit", "Filter care", "Active construction site", "Change coordination", "Pool school", "Estimated schedule", "Site access", "Permitting", "Change orders", "Service requests", "Payment methods", "Water service"] as const;
const termBodies = ["The project is a fictional portfolio sample and is not a binding agreement.", "Scope is limited to the listed project summary and selected specifications.", "Permits and inspections follow the applicable fictional project schedule.", "The customer keeps reasonable access to the work area available.", "Material colors and natural finishes may vary from samples.", "Changes are recorded before work proceeds.", "Payment is due according to the milestone schedule.", "Construction timing is an estimate, not a guarantee.", "Site conditions can require documented adjustments.", "The customer maintains utilities needed for installation.", "Safety barriers and code items are identified in the project record.", "Equipment selections are those shown in this document.", "Warranty information is illustrative and must be confirmed separately.", "Electronic signatures create the audit evidence shown below.", "A signed document is immutable in this demo.", "This compliance-ready presentation is not legal advice or certification."];
export type ContractFields = Record<string, string | number | boolean | null | undefined>;
export type ContractEnvelope = { version: typeof CONTRACT_DATA_VERSION; fields: ContractFields; acknowledgements: { number: number; title: string; contractorInitials: string; customerInitials: string; text: string }[]; terms: { title: string; body: string }[] };
const paymentNames = ["Permits & Plans", "Pool Shell", "Tile & Coping", "Deck & Pumps", "Screen Cage", "Pool Finish"];
const percentages = [2, 27, 25, 26, 11, 9];
const toMilestones = (raw: unknown) => { if (Array.isArray(raw)) return raw; if (typeof raw === "string") try { return JSON.parse(raw); } catch { return []; } return []; };
export function generateContractDefaults(input: { client?: Record<string, unknown> | null; build?: Record<string, unknown> | null; proposal?: Record<string, unknown> | null; date?: string }): ContractEnvelope {
 const client=input.client??{}, build=input.build??{}, proposal=input.proposal??{}, total=Number(proposal.total ?? build.salePrice ?? 0)-Number(proposal.discount ?? 0), milestones=toMilestones(proposal.milestones);
 const fields: ContractFields={contractDate:input.date??new Date().toISOString().slice(0,10),purchasedBy:String(client.name??""),installationAddress:[client.address,client.city,client.zip].filter(Boolean).join(", "),billingAddress:[client.address,client.city,client.zip].filter(Boolean).join(", "),jobPhone:String(client.phone??""),fax:"",others:"",legalDescr:"",subdivision:"",earthRemovalYes:Boolean(build.hasTreeRemoval),earthRemovalNo:!Boolean(build.hasTreeRemoval),additionalFillBuyer:false,additionalFillContractor:false,shuttleDigYes:false,shuttleDigNo:false,poolShape:"",poolSize:"",dimensionsLength:String(build.poolLength??proposal.poolLength??""),dimensionsWidth:String(build.poolWidth??proposal.poolWidth??""),poolLength:String(build.poolLength??proposal.poolLength??""),poolWidth:String(build.poolWidth??proposal.poolWidth??""),sqFt:"",minDepth:"",maxDepth:"",interiorQuartz:"",interiorPebble:Boolean(build.hasInteriorUpgrade),interiorColor:"",interiorOther:"",copingBNBR:false,copingPrecast:false,copingTrav:String(build.deckMaterial??proposal.deckMaterial??"")==="TRAVERTINE",copingCantil:false,tileAllowance:"",vacLineYes:false,vacLineNo:false,skimmerCount:"",filterSize:"",filterCartridge:"",filterDE:"",pumpHP:"",addlPumpHP:"",light100w:false,light300w:false,ledLightCount:"",ledLightYes:false,ledLightNo:false,cleaningEquipment:"",testKit:"",maintenanceFree:"",chlorinatorYes:false,chlorinatorNo:false,saltSystemYes:Boolean(build.isSalt),saltSystemNo:!Boolean(build.isSalt),infloorCleanYes:false,infloorCleanNo:false,benchDeepEnd:"",benchShallow:"",sunShelf:"",handRailYes:false,handRailNo:false,ladderYes:false,ladderNo:false,timeClock:"",automation:Boolean(build.hasAutomation),controlsJandy:"",controlsAqualogic:"",autoFillMech:"",autoFillElectronic:"",pilingsConcrete:"",pilingsHelicalYes:false,pilingsHelicalNo:false,pilingsDrivenBy:"",pilingsCappedBy:"",engineering:"",heaterHeatPump:Boolean(build.hasHeat),heaterHeatPumpSize:"",heaterGas:"",heaterGasSize:"",heaterTankHookup:"",heaterSolar:"",spaDim:Boolean(build.hasSpa)?"Included":"",spaElev:"",spaLight:"",jets:"",airBlowerHP:"",spaCoping:"",deckMaterial:String(build.deckMaterial??proposal.deckMaterial??""),deckType:String(build.deckMaterial??proposal.deckMaterial??""),deckColor:"",deckPattern:"",deckLanai:"",deckSkimcoat:"",poolWiringYes:false,poolWiringNo:false,screenType:Boolean(build.hasCage)?"Included":"",screenColor:"",screenDoors:"",screenGutters:"",screenPanroof:"",screenRailing:"",screenFence:"",sprinklersContractor:false,sprinklersBuyer:false,sodContractor:Boolean(build.hasSod),sodBuyer:false,spotSurveyYes:false,spotSurveyNo:false,spotSurveyBuyer:false,safetyCode:"",alarms:"",meshBarrierContractor:false,meshBarrierBuyer:false,item38:"",item39:"",item40:"",contractPrice:total,contractPriceDollars:total,depositAmount:"",roughShellAmount:"",tileRoughPlumbingAmount:"",deckEquipmentAmount:"",enclosureAmount:"",poolPrepInteriorAmount:"",buyerSignature1:"",buyerDate1:"",proposedBy:"",buyerSignature2:"",buyerDate2:"",contractorSignature:"Persistent Pools, LLC",contractorDate:""};
 let allocated=0; paymentNames.forEach((name,i)=>{const m=milestones.find((x: unknown)=>typeof x==="object"&&x!==null&&(x as {name?:string}).name===name) as {name?:string;amount?:number;percentage?:number}|undefined; const optionalMissing=name==="Screen Cage"&&milestones.length>0&&!m; const amount=optionalMissing?0:m?.amount??(i===5?total-allocated:Math.round(total*percentages[i]/100)); allocated+=Number(amount); fields[`payment${i+1}Name`]=name;fields[`payment${i+1}Amount`]=amount;fields[`payment${i+1}Percentage`]=optionalMissing?0:(m?.percentage??(i===5?Math.round(((total-allocated+Number(amount))/total)*100):percentages[i]));});
 return {version:CONTRACT_DATA_VERSION,fields,acknowledgements:ACKNOWLEDGEMENTS.map((title,i)=>({number:i+1,title,contractorInitials:"PP",customerInitials:"",text:`${title}: ${termBodies[i]}` })),terms:termBodies.map((body,i)=>({title:`${i+1}. ${ACKNOWLEDGEMENTS[i]}`,body}))};
}
export function normalizeContractData(raw: unknown): ContractEnvelope { const base=generateContractDefaults({}); if(!raw||typeof raw!=="object") return base; const r=raw as Partial<ContractEnvelope>; return {version:CONTRACT_DATA_VERSION,fields:{...base.fields,...(r.fields ?? raw as ContractFields)},acknowledgements:(r.acknowledgements?.length===16?r.acknowledgements:base.acknowledgements).map((x,i)=>({...base.acknowledgements[i],...x,title:(x as {title?:string}).title??ACKNOWLEDGEMENTS[i]})),terms:r.terms?.length===16?r.terms:base.terms}; }
export const KNOWN_CONTRACT_FIELD_KEYS = new Set(Object.keys(generateContractDefaults({ date: "2000-01-01" }).fields));
export function mergeKnownContractFields(fields: ContractFields, updates: unknown): ContractFields { if(!updates||typeof updates!=="object") return fields; const clean: ContractFields={...fields}; for(const [key,value] of Object.entries(updates as Record<string,unknown>)) if(KNOWN_CONTRACT_FIELD_KEYS.has(key)&&["string","number","boolean"].includes(typeof value)) clean[key]=value as string|number|boolean; return clean; }
export function publicContractProjection(contract: { id:string;status:string;contractData:string|null;signedByClient:string|null;signedDate:Date|string|null;signatureData?:string|null }) { const normalized=contract.contractData?normalizeContractData(JSON.parse(contract.contractData)):generateContractDefaults({}); const data={...normalized,fields:mergeKnownContractFields({},normalized.fields)}; let signatureEvidence: { signerName?: string; signedTimestamp?: string; documentHash?: string } | null = null; try { const signature=contract.signatureData?JSON.parse(contract.signatureData):null; if(signature) signatureEvidence={signerName:signature.signerName,signedTimestamp:signature.signedTimestamp,documentHash:signature.documentHash}; } catch {} return {id:contract.id,status:contract.status,contractData:JSON.stringify(data),signedByClient:contract.signedByClient,signedDate:contract.signedDate,signatureEvidence}; }
