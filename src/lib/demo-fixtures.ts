/** Stable identifiers for tour fixtures; never select records by visible copy. */
export const DEMO_FIXTURES = {
  haywardClientId: "demo-client-hayward",
  haywardProposalKey: "hayward-internal-proposal",
  haywardContractKey: "hayward-signed-contract",
} as const;
